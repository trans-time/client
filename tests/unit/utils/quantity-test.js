import quantity from 'client/utils/quantity';
import { module, test } from 'qunit';

module('Unit | Utility | quantity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = quantity();
  assert.ok(result);
});
