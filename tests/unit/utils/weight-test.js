import weight from 'client/utils/weight';
import { module, test } from 'qunit';

module('Unit | Utility | weight');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = weight();
  assert.ok(result);
});
