import fixNumber from 'client/utils/fix-number';
import { module, test } from 'qunit';

module('Unit | Utility | fix number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fixNumber();
  assert.ok(result);
});
