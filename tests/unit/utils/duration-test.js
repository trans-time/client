import duration from 'client/utils/duration';
import { module, test } from 'qunit';

module('Unit | Utility | duration');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = duration();
  assert.ok(result);
});
