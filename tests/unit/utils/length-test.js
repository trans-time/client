import length from 'client/utils/length';
import { module, test } from 'qunit';

module('Unit | Utility | length');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = length();
  assert.ok(result);
});
