{{#user-profile
  user=model
  follow=(route-action "follow")
  requestPrivate=(route-action "requestPrivate")
  unfollow=(route-action "unfollow")
  updateModel=(route-action "updateModel")
}}
  {{outlet}}
{{/user-profile}}
