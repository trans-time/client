{{yield}}

{{paper-button
  label=(t "general.cancel")
  onClick=(action "handleCancel")
}}

{{form.submit-button
  label=(t "general.submit")
  disabled=form.isInvalid
  raised=true
  primary=true
}}
