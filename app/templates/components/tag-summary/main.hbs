{{#each tags as |tag|}}
  {{#paper-button
    raised=true
    primary=tag.selected
    disabled=(not tag.isValid)
    onClick=(action "toggleTag" tag)
  }}{{tag.model.name}}: {{tag.amount}}{{/paper-button}}
{{/each}}

{{#link-to "users.user.timeline" (query-params direction="desc" tags=selectedTagIds)}}From end{{/link-to}}
{{#link-to "users.user.timeline" (query-params tags=selectedTagIds)}}From beginning{{/link-to}}
