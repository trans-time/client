{{paper-date-time-picker
  label=(t "users.attributes.birthday.label")
  placeholder=(t "users.attributes.birthday.placeholder")
  value=currentUser.user.birthday
  onClear=(action (mut clear))
  onSelection=(action "updateBirthday")
  classNames="birthday-form"
}}
