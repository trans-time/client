{{#if isLoaded}}
  {{#each ownComments as |comment|}}
    {{post-nav/chat/comment comment=comment}}
  {{/each}}
{{else}}
  <div class="post-nav-chat-spinner">
    {{fa-icon "spinner" pulse=true size=5}}
  </div>
{{/if}}
