{{yield}}

{{paper-button
  label=(t "general.cancel")
  onClick=(action "handleCancel")
}}

{{paper-button
  label=(t "general.submit")
  disabled=disabled
  raised=true
  primary=true
  onClick=(action "handleSubmit")
}}
