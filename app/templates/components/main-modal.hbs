{{#if componentPath}}
  {{#liquid-tether
    target="body"
    attachment="center center"
    targetAttachment="center center"
    class="main-modal"
  }}
    {{component componentPath}}
  {{/liquid-tether}}
{{/if}}
