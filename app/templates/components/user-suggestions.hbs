<ul id="infinite_content" class="follow-items">
  {{#each users as |user|}}
    {{follow-item user=user shouldShowFollowInterface=true disabled=true}}
  {{/each}}

  {{!-- {{infinity-loader infinityModel=users}} --}}
</ul>
