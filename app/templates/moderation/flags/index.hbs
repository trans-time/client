{{moderation-nav}}

<ul id="infinite_content" class="violation-report-items main-content">
  {{#each model as |flag|}}
    {{flag-item flag=flag}}
  {{/each}}

  {{infinity-loader infinityModel=model scrollable="#infinite_content"}}
</ul>
